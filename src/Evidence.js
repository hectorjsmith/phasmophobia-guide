
export default class Evidence {
    constructor(
        name,
        selected,
        rejected,
    ) {
        this.name = name
        this.selected = selected
        this.rejected = rejected
    }
}
